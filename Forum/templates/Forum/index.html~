{% load staticfiles %}

<link rel="stylesheet" type="text/css" href="{% static 'Forum/style.css' %}" />

</div>
{% for patient in patient_list %}
<form action="{{ patient.id }}" method="GET">
    <p>Patient n° {{patient.id}} <a href="patient">{{ patient }}</a></p>
{% endfor %}

{% if latest_question_list %}
    <ul>
    {% for question in latest_question_list %}
        {% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}
<form action="{% url 'Forum:vote' question.id %}" method="post">
<fieldset>
        {% csrf_token %}
        <legend>{{ question.question_text }}</legend>
        {% for choice in question.choice_set.all %}
            {% if question.type == 'radio' %}
                <input type="radio" name="choice" id="choice{{ forloop.counter }}"      value="{{ choice.id }}" />
                <label for="choice{{ forloop.counter }}">{{ choice.choice_text }}</label><br />
            {% else %}
                {{ choice }}: <input type="text" name="choice" id="choice{{ forloop.counter }}" value="{{ patient }}" />
            {% endif %}
        {% endfor %}

<p style="text-align: center;"><input type="submit" value="Submit"></p>
</fieldset>
</form>
        
    {% endfor %}
    </ul>

{% for reply in reply_list %}
{% for question in question_list %}
    <p>Question : {{ question.question_text }} --->  Réponse : {{ reply }}</p> 

{% endfor %}
{% endfor %}

</form>

{% else %}
    <p>No Patient are available.</p>
{% endif %}


