<div>
    <h1>Patient details : {{ patient }}</h1>    
</div>

{% block content %}
  {% for replies in question %}
    <h2>{{ replies }}</h2>
    <ul>
    {% for a in question.reply_set.all %}
      <li>{{ a }}</li>
    {% endfor %}
    </ul>
  {% endfor %}
{% endblock %}
